To Do

* Change navbar text color
* Some of the bootstrap alerts are in the wrong color (all are red?)
* Design Layout 
* Heroku app fails listings/new
* Sign up redirects to /listing/new ??
* Stripe works, but not integrated into the app flow
